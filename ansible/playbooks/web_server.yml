---
# Play 3: Configure the Webserver
- name: Setup and configure Nginx Webserver
  hosts: webservers
  become: yes
  vars_files:
    - ../vars/servers.yml
    - ../vars/ssh_keys.yml
  roles:
    - web_server